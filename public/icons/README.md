# Icon Assets

This folder contains icon files for the application.

## Usage

Icons can be referenced using `/icons/filename.svg` in your code.

For React components, consider using a library like `lucide-react` (already installed) or import SVG files directly.


